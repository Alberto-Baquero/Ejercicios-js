const game = {
  title: "The last of us 2",
  gender: ["action", "zombie", "survival"],
  year: 2020,
};

const { title } = game;
const { gender } = game;
const { year } = game;

console.log(title);
console.log(gender);
console.log(year);
